% Auto-converted from khan-exercises
\documentclass{article}
\usepackage{amsmath,amssymb}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\newcommand{\abs}[1]{\lvert #1\rvert}

\begin{document}
\section*{Direct and inverse variation}
\textbf{Question.} Which equation shows direct variation?

\textbf{Answer.} x, y = [[MULTIPLIER]] $\cdot$ 1

\textbf{Hints.}
\begin{itemize}
  \item x, y varies directly with 1 if x, y = k $\cdot$ 1 for some constant k.
  \item x, y = [[MULTIPLIER]] $\cdot$ 1 fits this pattern, with k = [[MULTIPLIER]].
  \item x, y varies directly with 1 if x, y = k $\cdot$ 1 for some constant k.
  \item If you divide each side of this expression by 1, you get $\frac{x, y}{1}$ = k for some constant k.
  \item $\frac{x, y}{1}$ = [[MULTIPLIER]] fits this pattern, with k = [[MULTIPLIER]].
  \item x, y varies directly with 1 if x, y = k $\cdot$ 1 for some constant k.
  \item If you divide each side of this expression by k, you get $\frac{1}{k}$ $\cdot$ x, y = 1.
  \item [[MULTIPLIER]] $\cdot$ x, y = 1 fits this pattern, with k = $\frac{1}{[[MULTIPLIER_IS_FRACTIONAL ? MULTIPLIER_VALUE : "\\frac{1}$\{"+MULTIPLIER\_VALUE+"\}"]]\}.
  \item x, y varies inversely with 1 if x, y = k $\cdot$ $\frac{1}{1}$ for some constant k.
  \item x, y = [[MULTIPLIER]] $\cdot$ $\frac{1}{1}$ fits this pattern, with k = [[MULTIPLIER]].
  \item x, y varies inversely with 1 if x, y = k $\cdot$ $\frac{1}{1}$ for some constant k.
  \item If you multiply each side of this expression by 1, you get x, y $\cdot$ 1 = k for some constant k.
  \item x, y $\cdot$ 1 = [[MULTIPLIER]] fits this pattern, with k = [[MULTIPLIER]].
  \item x, y varies inversely with 1 if x, y = k $\cdot$ $\frac{1}{1}$ for some constant k.
  \item If you divide each side of this expression by k, you get $\frac{x, y}{k}$ = $\frac{1}{1}$.
  \item Then you can take the inverse of each side to get $\frac{k}{x, y}$ = 1.
  \item [[MULTIPLIER]] $\cdot$ $\frac{1}{x, y}$ = 1 fits this pattern, with k = [[MULTIPLIER]].
\end{itemize}
\end{document}
